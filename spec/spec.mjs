import { createArray } from "../src/functions.js";

describe("Array", function () {
  describe("createArray", function () {
    if('Should return [0,0,0]', function() {
        expect()
    }) {

    }
  });
});
